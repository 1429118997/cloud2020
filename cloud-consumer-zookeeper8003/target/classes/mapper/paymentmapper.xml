<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloud.mapper.PayMentMapper">
 <resultMap id="basicResultMapper" type="PayMent">
     <id property="id" column="id" jdbcType="BIGINT"></id>
     <result property="serial" column="serial" jdbcType="VARCHAR"></result>
 </resultMap>

    <select id="queryPayMentForId" resultMap="basicResultMapper" parameterType="long">
        select * from payment where id=#{id}
    </select>

    <insert id="insertPayMent"  parameterType="Payment" useGeneratedKeys="true" keyProperty="id">
        insert into payment(serial) value(#{serial})
    </insert>
</mapper>